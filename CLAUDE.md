# CLAUDE.md

## このプロジェクトは何か

**性格は「自分が覚えたことが、可視化できること」。** 迷ったらここに戻る。

このアプリは鏡であって、テストする道具でも管理する道具でもない。
だから鏡として**欠けても歪んでもいけない**。

- 欠けない ＝ 覚えていないものも含めて全体が見える（網羅性）
- 歪まない ＝ 覚えたつもりを本当に覚えたことと混ぜない（確認カード）

ペース計算や受験日の見込みも持っているが、**それは主役ではない**。
画面でいちばん大きいのは、いつも「自分が積み上げたもの」であるべき。

そのうえで、機能としては
合格までの道のりを可視化して、忘れる前に復習させ、間に合うペースに導くツール。
漠然とした不安を、名前のついた作業に変えることが目的。
React + TypeScript + Vite の SPA。バックエンドはまだ無い。

コンセプトは `docs/concept.html`（図つき）、各画面・ロジックの詳細は `README.md`。
作業前に両方読むこと。

## コマンド

```bash
npm install
npm run dev     # http://localhost:5173
npm run build   # tsc && vite build
npm run lint    # eslint（警告0で通る状態を維持する）
npm test        # vitest run（logic/ の判定条件を固定している）
```

環境変数は無い。`.env` を要求する変更は入れない。

## 崩してはいけない前提

この5つはプロダクトの主張そのもの。変更したくなったら、実装する前に相談すること。

1. **一回の申告で到達度を動かさない。** 間隔をあけて複数回、同じ結果が続いたときにだけ動く。
   このアプリは問題を持たないので、入力は必然的に申告になる。だからこそ
   単発では信用しない。読んだだけで「できた」を振った観点は、間隔が空いた2回目で
   落ちる ── 時間が申告を検証してくれる。
   「やった」チェックボックスを置かないのは、それが一回で確定してしまうから。
   到達度は `logic/mastery.ts` の計算結果としてのみ存在する。

   **ただし、時間が検証できるのは「忘れたこと」だけ。**「逆で覚えていた」は
   検証できない。逆に覚えている人は流暢に確信をもって思い出すので、即答して
   「完璧」に振り、保持日数が最大 ×1.8 まで伸びていちばん出てこなくなる。
   忘却は自己修正するが、勘違いは安定していて自己修正しない。
   そこで `CHECKS`（確認カード）で時々だけ実際に突き合わせる。
   **正解と突き合わせない限り勘違いは検知できない**ので、ここは省略できない。
2. **観点の依存関係を UI の構造として使う。** `prereqIds` がマップの形であり、
   開放条件でもある。順序は `logic/graph.ts` が導出するので、画面側で並べ替えない。
   マップを自由配置・カンバン・カテゴリ一覧に変えない。

   **ただし一本道にはしない。** 実データの依存関係は、ほぼ独立した4本の流れ
   （数と式20 / 図形19 / 関数10 / データ6）と、まれに架かる4本の橋でできている。
   根は3つあり、常に平均5個・最大11個が同時に開く。これを1列に潰すと、
   (a) 独立した観点の並び順が `curriculum.ts` の記述順で決まってしまい、
   執筆の都合が教育的順序として提示される。(b) 領域をまたぐ4本の橋
   （数と式→関数が3本、数と式→図形が1本）が「たまたま隣」と区別できなくなる。
   マップは地図の形にせず、観点ごとに「何を土台にしているか」を名前で見せる。

   **そして依存関係でロックしない。** どの順に覚えるかは本人の自由であり、
   学習の順序を決めているのは学校や本人であってこのアプリではない。
   授業で二次関数をやっている生徒を一次関数で止めるのは筋が違う。
   `needsFoundation` は禁止のフラグではなく「ここが苦しいなら、たぶん
   土台のほうが原因」という説明に使う。候補から外してはいけない。
3. **ユーザーに「何をやるか」を選ばせない。** 優先度の判断は `logic/pace.ts` の
   `prioritize()` に集約する。画面側に選定ロジックを書かない。
   **並び順は工程がつながるようにする。** 深さ順だけで並べると全体を横なめする
   幅優先になり、一次関数の翌日に平面図形が出て二次関数はずっと先、という順になる。
   `prioritize()` は直前に触った観点の続きを先に出し、`buildQueue()` は
   1セッションの中で鎖を歩く。工程がつながっているほうが学習効率が高い。
   これは `prioritize()` の仕事であって、マップの形とは無関係。
   レーンが何本あっても次の1つは1つに決まる。かつて一本道にしていたのは
   「1つの次の行動」と「1本の構造」を混同していたため。
4. **踏破と鮮度を混ぜない。** 踏破（`everMastered`）は一度立つと下りない。
   忘却で減らすのは鮮度（`freshness`）だけ。受験勉強はすごろくなので、
   忘れたからといってコマまで戻さない。進捗の数字を後退させる変更は入れない。
5. **観点を忘却の対象から外す機能を作らない。** 「完璧に覚えた」で復習を打ち切れる
   ようにすると、「Aは完璧だから復習しない → 本番でAを忘れている」という、
   このアプリが検知しようとしている失敗を利用者の手で確定させるスイッチになる。
   自信の申告は**保持日数の係数**として受け取る（`PERFECT_BONUS`）。
   **項目を列から消す信号は入れない。間隔を動かすだけの信号なら入れてよい。**
   後者は判定が甘くても、遅れて戻ってきて実際の結果で上書きされる。

## 参考書ではない

**問題文と答えは持たない。** 学習そのものは教科書・問題集の側でやる。
このアプリがやるのは、中学・高校で学ぶことを観点として一覧にして、
次にどれを触るか指し示し、忘れる前に戻すことだけ。

だから `KnowledgePoint` に持たせるのは `ask`（「〜できる？」の問いかけ）であって、
問題と解答ではない。カードを見た瞬間に頭の中で答えを作らせるための文にする。
解説・例題・答え合わせの機能を足したくなったら、その前に相談すること。

**例外は `CHECKS`（確認カード）だけ。** 自己申告では「逆で覚えていた」を
検知できないので、監査用に答えを持つ。線引きは固定する。

- **1観点につき最大1問。** 増やすと問題集になる
- **解説を持たない。** 学び直しは教科書・問題集の側
- **全観点には置かない。** 「逆で覚えやすいところ」にだけ置く
- 誤答の選択肢に必ず「逆」を入れる。そこを選ぶ人を見つけるためのカードなので
- 1セッションに1枚まで（`pickAudit()`）

## UI の方針

- 対象は中学生〜高校生、男女とも。**様々な学力の子が同じ画面を見る**
- ライトテーマ。ダークにすると学校が配る管理ツールに見える
- 丸ゴシックは使わない（かわいい側に振れる）。**親しみは角丸と厚みで出し、
  文字は太いゴシックで強く保つ**。主色はオレンジ1色、土台の文字色は寒色
- **見出しに全体の分母（55個）を置かない。** 同じ 3/55 が上位層には燃料に、
  下位層には壁になる。分母はいつも「いま埋めている単元」にする（`logic/units.ts`）
- 判定基準は `docs/success-criteria.md` の7つ。迷ったらそこで判定する
- ボトムタブは3つ（今日 / マップ / じぶん）。増やさない
- 「今日」に出す数字は着地予測ひとつだけ。分析的な指標は「じぶん」に置く
- 判定の入力はスワイプカード。左右で「あやしい / できた」、上で「完璧」。
  一覧 + チェックボックスの形に戻さない
- 今日ぶんを踏み終えたら、はっきり終わりだと言い切る。終わりが来ない画面にしない

## コードの構成

```
src/
  logic/graph.ts     依存関係から道の順序・深さ・子孫数を出す
  logic/mastery.ts   到達度の判定・鮮度の予測・開放条件・志望校での絞り込み
  logic/pace.ts      踏破率とペース計算・優先度づけ・セッション生成
  logic/units.ts     単元ごとのまとまり・図鑑の並び（手の届く分母）
  logic/*.test.ts    判定条件を固定するテスト
  data/curriculum.ts 観点マスタ（中学数学55観点）と志望校マスタ
  hooks/             localStorage 永続化
  screens/           3つのタブ
  components/        スワイプセッション・志望校設定・ナビ
```

計算は `logic/` に閉じ込め、画面は結果を表示するだけにしている。
`PointStatus` が画面とロジックの境界の型。

判定条件（連続正答2回・3日間隔・保持日数の予測・今日ぶんの目標・セッションの構成比）を
変えるときは、先に `logic/*.test.ts` を直してから実装する。テストが仕様書を兼ねている。

数字の置き場所は `logic/` の先頭の定数にそろえる。画面側に閾値を書かない。

## 注意点

- `data/curriculum.ts` は中学数学55観点の開発確認用データ。領域構成と単元の並びは
  中学校学習指導要領（数学）に沿わせているが、観点への割り方・頻出度・学校名は
  すべて作り物。**本物の入試情報として提示する文言を書かないこと。**
  実データを入れる場合は出典を伴わせる
- 依存の深さ（最大10）と学年をまたぐ辺（20本）はこのデータの価値そのもの。
  観点を足し引きするときに平たくしない。`graph.test.ts` が下限を固定している
- CSS はコンポーネントごとではなく `src/app.css` に一括で置いている
- `.study-app` 等の旧クラス名は残っていない。ルートは `.app`

## 現状と、次にやると効くこと

プロトタイプとして動く状態。ビルド・lint・テストともにクリーン。

- **数学1科目しかない。** 科目を増やすのが次の本体作業。一次ソースは学習指導要領
  （公開・全国共通で、出典を示せる）。科目単位で積む作業で、一度に書き下ろさない
- 国語は現代文に依存関係がほぼ言えず、`analyzeGraph` が全部を depth 0 に並べる。
  前提2の限界がどこかを見るには効くが、入れると道が平たくなるので判断が要る
- 志望校データが本物ではない。頻出度は過去問への観点タグ付けから作るのが本来の道筋
- 永続化が localStorage のみ。端末を変えると消える
- 保持日数の係数（間隔 × 2.2、忘却で × 0.6、反応時間で ×0.9〜1.2、完璧で ×1.5）は
  仮置き。実データで調整する前提
- 「もう分かっている範囲を飛ばす」診断ステップは未着手。依存の鎖をさかのぼる形なら
  8問前後で9割の当たりがつく（3問で55観点中38個）。ただし1回の正答で踏破を
  与えてはいけない。動かしてよいのは開放と優先度だけ
- ペースが足りないときに範囲を削る提案は入れていない。`Target` に永続的なフラグが増え、
  出題範囲を決める経路が `emphasis` と2つになるため、設計してから入れる
- 他の受験生との「ぼんやりしたランク」は検討中。バックエンドと、
  未成年のデータ収集の設計が前提になる（`docs/concept.html` に記録）
